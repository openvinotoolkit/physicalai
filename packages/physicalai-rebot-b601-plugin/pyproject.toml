[build-system]
requires = ["hatchling<1.30", "hatch-vcs"]  # cap <1.30: hatchling>=1.30 emits Metadata 2.5, rejected by PyPI/twine (pypi/warehouse#19083)
build-backend = "hatchling.build"

[project]
name = "physicalai-rebot-b601-plugin"
dynamic = ["version"]
description = "Third-party Seeed reBot B601 robot arm plugin for PhysicalAI"
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
dependencies = [
    "physicalai>=0.1.1",
    "physicalai-studio-plugin>=0.2.0",
    "numpy>=1.24",
    "loguru>=0.7",
    "motorbridge>=0.4.4",
    "pydantic>=2.0",
]

[project.optional-dependencies]
tests = ["pytest"]

[project.urls]
Homepage = "https://github.com/openvinotoolkit/physicalai"
Repository = "https://github.com/openvinotoolkit/physicalai"
Issues = "https://github.com/openvinotoolkit/physicalai/issues"

[project.entry-points."physicalai.studio.catalog_plugins"]
rebot-b601 = "physicalai_rebot_b601_plugin.studio_catalog:register_physicalai_studio_plugin"

[tool.hatch.version]
source = "vcs"
tag-pattern = '^physicalai-rebot-b601-plugin-v(?P<version>\d+(?:\.\d+)*)$'
raw-options = { search_parent_directories = true, git_describe_command = "git describe --dirty --tags --long --match physicalai-rebot-b601-plugin-v*" }

[tool.hatch.build.targets.wheel]
packages = ["src/physicalai_rebot_b601_plugin"]

[tool.hatch.build.targets.wheel.force-include]
"urdf" = "urdf"

[tool.hatch.build.targets.sdist]
include = [
    "src/physicalai_rebot_b601_plugin/**",
    "urdf/**",
    "README.md",
    "pyproject.toml",
    "LICENSE",
]
exclude = [
    "examples/**",
]
